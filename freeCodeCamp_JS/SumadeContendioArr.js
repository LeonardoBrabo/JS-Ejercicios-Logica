// Configuración
const myArr = [2, 3, 4, 5, 6];

// Cambia solo el código debajo de esta línea
let total = 0;
for (let i=0; i < myArr.length; i++){  //el error estaba en que escribir lenght y era length.
  total += myArr[i];
}
console.log(total);